# https://taskfile.dev

version: "3"

vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
      - cd script && tinygo build -o main.wasm -target=wasm-unknown -gc=precise main.go
      - cargo r -r
    silent: true
